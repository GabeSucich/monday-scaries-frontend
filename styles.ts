import { StyleSheet } from "react-native"

export const GlobalStyleAttrs = {
    backgroundColors: {
      neutral: "#8a9ab580",
      green: "#56825280",
      red: "#8c3e3f80",
      lightGreen: "#8b9a71"
    },
    buttonColors: {
      green: "#387337",
      blue: "#2166a3"
    }
  }
  
  export const GlobalStylesheet = StyleSheet.create({
    box: {
      borderRadius: 5,
      borderColor: "#0f0b0b50",
      borderWidth: 2,
    }
  })